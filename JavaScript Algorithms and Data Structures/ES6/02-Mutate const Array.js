const s = [5, 7, 2];
function editInPlace() {
  "use strict";
  s[0] = 2;
  s[1] = 5;
  s[2] = 7;
} //Code of "Get a Hint", was trying with a for loop since i saw that as a automatic sorting but guess this is the right thing
editInPlace();